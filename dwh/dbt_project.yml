name: 'breast_cancer_project'
version: '1.0'
config-version: 2

profile: 'breast_cancer_project'  # trùng tên trong profiles.yml

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
